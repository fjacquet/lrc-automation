---
phase: 04-ux-and-docs
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - docs/prd.md
  - CHANGELOG.md
autonomous: true
requirements:
  - UX-05
  - UX-06
user_setup: []

must_haves:
  truths:
    - "docs/prd.md names macOS and Windows as both primary target platforms and explicitly states Linux is CI-only"
    - "CHANGELOG.md has a complete ## [0.6.0] entry covering all changes from phases 1-4"
    - "The [0.6.0] CHANGELOG entry includes Added, Fixed, and Changed subsections"
  artifacts:
    - path: "docs/prd.md"
      provides: "Updated PRD naming macOS + Windows as target platforms, Linux as CI-only"
      contains: "Windows"
    - path: "CHANGELOG.md"
      provides: "v0.6.0 release entry"
      contains: "0.6.0"
  key_links:
    - from: "CHANGELOG.md"
      to: ".planning/STATE.md decisions log"
      via: "All Phase 1-4 changes enumerated in Added/Fixed/Changed sections"
      pattern: "0\\.6\\.0"
---

<objective>
Update docs/prd.md to reflect the v0.6.0 multiplatform scope (macOS + Windows primary targets, Linux CI-only), and write the complete v0.6.0 CHANGELOG entry covering all changes from all four phases.

Purpose: PRD accurately reflects the shipped product's platform support; CHANGELOG gives users and contributors a complete record of what changed in v0.6.0.
Output: Updated docs/prd.md with Windows platform statement; CHANGELOG.md with complete [0.6.0] section.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ux-and-docs/04-RESEARCH.md
@docs/prd.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/prd.md for Windows platform scope</name>
  <files>docs/prd.md</files>
  <action>
    Read the full current content of docs/prd.md before editing.

    Make the following targeted changes (do not rewrite the entire document):

    1. **Section 4 (Users / Target User):** Find the sentence describing the primary user platform. Replace "on macOS" (or "macOS only") with "on macOS or Windows". If there is a bullet or paragraph describing platform support, add:
       - "Secondary platform: Windows. All core features are supported. The `[geo]` extra (offline reverse geocoding) is macOS/Linux only due to a missing Windows wheel for `reverse_geocoder`."

    2. **Non-Functional Requirements — NF-3 (or equivalent compatibility requirement):** Find the current text that says something like "Python 3.12+, tested on macOS; should work on Linux". Replace with:
       "Python 3.12+; target platforms are macOS and Windows. Linux is supported as CI-only (Lightroom Classic does not run on Linux)."

    3. **Open Questions section (if present):** If there is an open question about "Windows path support" or similar, mark it as resolved. Change status from open to resolved/answered, and add a brief note: "Resolved in v0.6.0: full Windows support shipped."

    Do not change any other sections. Preserve all existing structure and formatting.
  </action>
  <verify>
    <automated>grep -i "windows" /Users/fjacquet/Projects/lrc-automation/docs/prd.md | wc -l</automated>
  </verify>
  <done>docs/prd.md mentions Windows as a target platform in at least the Users section and the platform/compatibility requirement. The Linux CI-only constraint is documented. No other sections changed.</done>
</task>

<task type="auto">
  <name>Task 2: Write complete v0.6.0 CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <action>
    Read the full current content of CHANGELOG.md before editing. Also run `git log --oneline` from the project root to see all commits since the Phase 1 branch point and cross-reference against the research file's compiled change list.

    Add a new `## [0.6.0] - 2026-03-06` section at the top of the changelog (after any header/preamble but before the previous release entry). Follow the existing CHANGELOG format exactly.

    The entry must contain these three subsections with the following content (use the exact wording from the research file, adapted to the project's CHANGELOG style):

    ### Added
    - **Windows support**: macOS and Windows are now both primary target platforms
    - `--catalog` / `-c` flag is now optional; the tool auto-discovers the default Lightroom Classic catalog at `~/Pictures/Lightroom/` (macOS) or `%USERPROFILE%\Pictures\Lightroom\` (Windows) when not specified
    - Cross-platform Lightroom process detection via `psutil`: detects `Adobe Lightroom Classic` (macOS) and `Lightroom.exe` (Windows), replacing the macOS-only `pgrep` subprocess
    - `pathFromRoot` SQL writes now always use forward slashes (`path.as_posix()`) so Lightroom can locate folders after a move on Windows
    - `PermissionError` retry loop in `executor.py` for transient antivirus scan locks on Windows
    - `.gitattributes` with `* text=auto eol=lf` to prevent CRLF failures on Windows CI checkout
    - CI matrix expanded to `ubuntu-latest`, `macos-latest`, and `windows-latest` runners for Python 3.12 and 3.13
    - SBOM (Software Bill of Materials) generated at release time via `anchore/sbom-action@v0` and attached to GitHub releases
    - ADR-007 documenting multiplatform decisions: psutil process detection, `as_posix()` SQL writes, darwin-only AppleDouble guard, SBOM generation, SQLite URI forward-slash fix, catalog auto-discovery
    - Windows installation and first-run section in README and `docs/usage.md`
    - `docs/prd.md` updated to name macOS and Windows as target platforms

    ### Fixed
    - `CatalogConnection.open()`: SQLite URI now uses forward slashes on Windows (`_path_to_sqlite_uri` converts `Path.as_posix()` result), fixing "unable to open database" error on Windows
    - Opening a Mac-origin catalog (with `/Volumes/` absolute paths) on Windows now prints a human-readable warning and exits rather than crashing
    - `AppleDouble` (`._*`) file cleanup silently skipped on non-macOS platforms (no errors, no spurious log entries)
    - `reverse_geocoder` moved back to optional `[geo]` extra dependency (fixes packaging regression from v0.5.0)

    ### Changed
    - `setup-uv` bumped to v7 with `enable-cache: true` in both `ci.yml` and `release.yml`
    - `ci.yml`: individual `uv run` steps replace `make check` (make unavailable on Windows runners)
    - `release.yml`: individual `uv run` steps replace `make check` for consistent CI step visibility

    After inserting the new entry, verify the CHANGELOG still has the previous release entry below it and the format is consistent.
  </action>
  <verify>
    <automated>grep "0.6.0" /Users/fjacquet/Projects/lrc-automation/CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md has a `## [0.6.0]` section at the top with Added, Fixed, and Changed subsections. All Phase 1-4 changes are listed. Previous entries are intact below it.</done>
</task>

</tasks>

<verification>
- `grep -i "windows" docs/prd.md` returns lines in the users and NF sections
- `grep "0.6.0" CHANGELOG.md` returns the release header line
- `grep -c "psutil\|as_posix\|darwin\|SBOM\|gitattributes\|auto-discover" CHANGELOG.md` returns 5+ matches
- `uv run pytest -x -q` passes (no regressions)
- `uv run ruff check .` passes
</verification>

<success_criteria>

- docs/prd.md: Windows appears as target platform; Linux is documented as CI-only; NF compatibility requirement updated
- CHANGELOG.md: `## [0.6.0] - 2026-03-06` section exists with Added (11+ items), Fixed (4 items), Changed (3 items)
- No previous CHANGELOG entries removed or corrupted
- Full `make check` passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-ux-and-docs/04-03-SUMMARY.md`
</output>
